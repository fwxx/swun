Swun is a very simple Software Update notifier.
It places itself as an an icon into the notification area of the
desktop environment and informs you about possible updates.
Swun requires a running slackpkg configuration
(e.g. working slackpkg update)!
